<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:ABA_Therapy_Tracker.ViewModels"
             x:Class="ABA_Therapy_Tracker.Views.Tracking"
             Title="{Binding Title}">

    <ContentPage.BindingContext>
        <vm:TrackingViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <Label Text="Patient"></Label>
            <Picker Title="Patient"
                    ItemsSource="{Binding Patients}"
                    ItemDisplayBinding="{Binding Name}">
            </Picker>
            <Grid>

            </Grid>
            <Button Text="Button 1" VerticalOptions="Start" Margin="5" HorizontalOptions="Center"></Button>
            <Button Text="Button 2" VerticalOptions="Start" HorizontalOptions="Center"></Button>
            <Button Text="Button 3" VerticalOptions="StartAndExpand" HorizontalOptions="Center"></Button>


        </StackLayout>
    </ContentPage.Content>
</ContentPage>