import Objective from "../../models/Objective";

const PatientBehaviorTrendData: { objectives: Objective[] } = {
  objectives: [
    {
      description: "Tantrum",
      objectiveId: "unique-id-1",
      type: "Counter",
    },
  ],
};

export default PatientBehaviorTrendData;
